import { BotInteractionPanel } from '@/components/BotInteractionPanel';

export default function BotInteractions() {
  return (
    <div className="min-h-screen bg-slate-900">
      <BotInteractionPanel />
    </div>
  );
}